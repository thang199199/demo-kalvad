export class BookClass {
  title: string;
  year: number;
  author: string;

  constructor(title: string, year: number, author: string) {
    this.title = title;
    this.year = year;
    this.author = author;
  }
}
